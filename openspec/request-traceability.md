# Request Traceability Matrix

This matrix manually traces each sample request in `mock_data/requests.json` through the four tool
specifications:
- `check_budget`
- `check_vendor_duplication`
- `check_policy_compliance`
- `assess_risk`

All requests call all four tools. Final resolution uses decision priority: **escalate > deny > approve**.

## Tool Output Shorthand

- Budget: `within` or `over` (with overage amount)
- Vendor Duplication: `ok` or `deny(POL-001)` with conflict vendors
- Policy Compliance: `none` or violation list with forced decisions
- Risk: `low | medium | high | critical`

## Traceability Table

| Request | Tools Called | check_budget return | check_vendor_duplication return | check_policy_compliance return | assess_risk return | Decision rule applied | Final decision | Rationale text (expected) |
|---|---|---|---|---|---|---|---|---|
| REQ-001 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-001`: remaining 187550, amount 24000, overage 0) | `ok` (amount <= 25000 threshold; no POL-001 deny) | `none` | `low` (V-002 active contract, no compliance flag) | No escalate signals, no deny signals -> approve | approve | "Budget check passed for CC-001 and amount is within remaining funds. No duplication violation and no policy violations were triggered. Vendor risk is low with an active contract and no compliance flag." |
| REQ-002 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-004`: remaining 210800, amount 38400, overage 0) | `ok` (requested vendor V-005 is contracted in category; no single-source breach) | `none` (manager threshold can be process-gated, not forced deny/escalate in this fixture) | `low` (V-005 active contract) | No escalate signals, no deny signals -> approve | approve | "Request amount is within cost center budget. Vendor is contract-valid for hardware and no enforced policy violation was returned. Risk profile is low, so the request can be approved." |
| REQ-003 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-006`: remaining 348700, amount 8500, overage 0) | `ok` (amount <= 25000) | `none` | `low` (V-007 active contract) | No escalate signals, no deny signals -> approve | approve | "Budget and category checks passed with no overage or duplication concerns. Policy compliance returned no violations. Vendor risk is low, supporting approval." |
| REQ-004 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-008`: remaining 83600, amount 9960, overage 0) | `ok` (amount <= 25000) | `none` | `low` (V-013 active contract) | No escalate signals, no deny signals -> approve | approve | "The request remains within budget and below single-source threshold behavior. Policy scan found no violations. Vendor is low risk with an active contract." |
| REQ-005 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-001`: remaining 187550, amount 14200, overage 0) | `ok` (amount <= 25000) | `none` | `low` (V-011 active contract) | No escalate signals, no deny signals -> approve | approve | "Budget sufficiency was confirmed for CC-001. No duplication or policy violation was detected for this security purchase. Vendor risk is low, so approval is appropriate." |
| REQ-006 | budget, vendor_duplication, policy_compliance, risk | `over` (`CC-003`: remaining 6900, amount 11200, overage 4300) | `ok` (amount <= 25000) | `POL-008 -> deny` (budget overage prohibition) | `low` (V-007 active contract) | Deny signal present (budget overage / POL-008), no higher escalate signal -> deny | deny | "Budget check failed: request exceeds remaining CC-003 budget by 4300. Policy compliance also triggers POL-008, which forces deny for over-budget requests. Vendor risk is low but does not override denial." |
| REQ-007 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-010`: remaining 8550, amount 5400, overage 0) | `ok` (amount <= 25000) | `POL-005 -> deny` (expired contract vendor) | `high` (V-010 expired contract) | Deny signal present (POL-005 and high risk), no escalate signal -> deny | deny | "Although budget is sufficient, policy compliance reports POL-005 because the vendor contract is expired. Risk assessment is high for the same contract-status reason. Deny is required." |
| REQ-008 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-004`: remaining 210800, amount 28500, overage 0) | `deny(POL-001)` conflicts: V-001 (CTR-2024-0041), V-003 (CTR-2023-0198) | `none` | `medium` (V-012 contract_status none) | Deny signal present (POL-001 single-source violation), no escalate signal -> deny | deny | "Budget is available, but vendor-duplication check found active contracted vendors in office_supplies while requested vendor is non-contracted above the POL-001 threshold. Policy check adds no higher-priority escalation. Deny is required due to single-source restriction." |
| REQ-009 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-005`: remaining 5500, amount 2550, overage 0) | `ok` (amount <= 25000) | `POL-004 -> deny` (catering prohibited) | `medium` (V-017 no contract) | Deny signal present (POL-004), no higher escalate signal -> deny | deny | "Budget is sufficient, but policy compliance triggers POL-004 because catering spend is prohibited. Vendor risk is medium due to no contract, which does not supersede the explicit deny policy. Request is denied." |
| REQ-010 | budget, vendor_duplication, policy_compliance, risk | `over` (`CC-009`: remaining 37900, amount 49600, overage 11700) | `ok` (no other active fleet_parts contract conflict) | `POL-003 -> escalate` (within 5% of 50000 director threshold) and `POL-008 -> deny` (over budget) | `low` (V-013 active contract) | Escalate signal exists (POL-003 near-threshold), which overrides deny by priority -> escalate | escalate | "Budget overage is present, which is a deny-level condition, but policy compliance also raises a director-threshold escalation signal for near-threshold spend. Under decision priority, escalate overrides deny. Escalate for director review with budget overage context." |
| REQ-011 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-001`: remaining 187550, amount 35000, overage 0) | `ok` (no conflicting active professional_services contract forcing POL-001 deny) | `POL-006 -> escalate` (compliance-flagged vendor) | `critical` (V-006 compliance flag true) | Escalate signal exists (POL-006 and critical risk) -> escalate | escalate | "Budget is sufficient, but policy compliance triggers POL-006 due to an active compliance flag on the vendor. Risk assessment returns critical for the same condition. Escalate is mandatory for Legal and Compliance review." |
| REQ-012 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-002`: remaining 51250, amount 4620, overage 0) | `ok` (amount <= 25000) | `none` (staffing hours > 40 but vendor is contracted, so POL-007 not violated) | `low` (V-014 active contract) | No escalate signals, no deny signals -> approve | approve | "Budget check passed and no single-source violation applies at this amount. Policy compliance found no violation because staffing uses the contracted enterprise vendor. Risk is low, so approve." |
| REQ-013 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-007`: remaining 18000, amount 14400, overage 0) | `ok` (amount <= 25000; no active-contract conflict forcing deny) | `none` | `medium` (V-015 contract_status none) | No escalate signals, no deny signals -> approve | approve | "The request is within budget and no policy violation was identified for training spend. Vendor duplication check did not trigger POL-001. Risk is medium due to no contract, but with no forced deny/escalate signals the decision is approve." |
| REQ-014 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-006`: remaining 348700, amount 47500, overage 0) | `ok` (requested vendor is contracted; no single-source breach) | `POL-003 -> escalate` (within 5% of 50000 director threshold) | `low` (V-016 active contract) | Escalate signal exists (POL-003 near-threshold) -> escalate | escalate | "Budget and contract checks pass, but policy compliance marks the amount as near the director threshold under POL-003 handling. Decision priority requires escalation when an escalate-level policy is present. Escalate for director awareness before commitment." |
| REQ-015 | budget, vendor_duplication, policy_compliance, risk | `within` (`CC-005`: remaining 5500, amount 1200, overage 0) | `ok` (amount <= 25000; no covered single-source conflict) | `none` | `medium` (V-004 contract_status none) | Baseline: no escalate/deny signal -> approve; optional conservative variant -> escalate | approve (baseline) | "Budget remains sufficient and no policy or duplication violation is triggered for courier services. Risk is medium because the vendor has no active contract, but no forced escalation rule is present in baseline specs. Approve by default; teams may optionally treat this as a conservative escalation case." |

## Notes For Session 4 Test Assertions

- Use this matrix as the expected baseline for deterministic assertions on tool outputs and final decisions.
- For `REQ-015`, if the team chooses conservative prompt behavior, allow `{approve, escalate}` as acceptable.
- If a tool returns an error in any test, assert final decision is `escalate` and rationale includes the error context.
